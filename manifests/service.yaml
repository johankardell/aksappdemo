apiVersion: v1
kind: Service
metadata:
  name: aksdemo
  namespace: aksappdemo
  labels:
    app.kubernetes.io/name: aksdemo
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: aksdemo
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000