apiVersion: v1
kind: Service
metadata:
  name: test
  namespace: default
  labels:
    app.kubernetes.io/name: test
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: test
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000